# 2026-05-29 — shadcn/ui Setup, Drawer Form, and Filterable/Responsive Register

Activity log for May 29th documenting the transition to `shadcn/ui`, the responsive transaction entry form layout, and the detailed, filterable transaction register with a mobile card-list fallback.

---

## Technical Overhaul

### 1. shadcn/ui & Tailwind CSS v4 Configuration
*   Initialized shadcn CLI using `--defaults` and verified it configured tailwind OKLCH variables within `@theme inline` inside [globals.css](file:///Users/kevin/Documents/antigravity/ledger/app/globals.css).
*   Added core library components:
    *   `Button` and `Dialog` (powered by `@base-ui/react` primitives)
    *   `Drawer` (powered by `vaul` bottom-sheet primitives)
    *   `Input` and `Select` components
*   Created utility class merging helper [utils.ts](file:///Users/kevin/Documents/antigravity/ledger/lib/utils.ts).
*   Created screen size detector hook [use-media-query.ts](file:///Users/kevin/Documents/antigravity/ledger/hooks/use-media-query.ts).

### 2. Responsive Ingestion Form
*   Modified [TransactionEntryForm.tsx](file:///Users/kevin/Documents/antigravity/ledger/app/components/TransactionEntryForm.tsx) to check media query widths:
    *   **Desktop screens (`>= 768px`):** Renders centered `Dialog` modal overlay.
    *   **Mobile screens (`< 768px`):** Renders slide-up bottom `Drawer` with draggable gesture handles.
*   Preserved all existing autocomplete, validation, state logic, and database insertion API bindings.

### 3. Filterable & Sorting Register
*   Modified [RegisterClient.tsx](file:///Users/kevin/Documents/antigravity/ledger/app/register/RegisterClient.tsx):
    *   **Filter Bar:** Added text input query field and dropdown selectors for Account, Category, Budget, and Cleared/Reconciled status to instantly filter ledger data on the client side.
    *   **Column Sorting:** Table column headers are now clickable button triggers that toggle sort order (`asc` / `desc`) on dates, payees, categories, budgets, amounts (combined debits/credits), and running balances, displaying `▲` / `▼` symbols on the active column.
    *   **Mobile Card View:** Hide table layout on mobile viewports. Renders a vertical layout of transaction summary cards. Clicking a card expands it to display notes, invoice date, source, and quick action buttons (Flag, Clear, Reconcile).
