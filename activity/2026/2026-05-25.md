# 2026-05-25

## Plan Page — Complete

Finished the Plan page (YNAB-style budget planning view).

### What was built

**`app/plan/PlanClient.tsx`** — client component for the Plan page:
- 3-column layout: dark navy sidebar (Plan active) | center table | right summary panel
- Month navigator: `‹ May 2026 ›` buttons push `?month=YYYY-MM` to URL
- Status banner (green/blue/red) based on total available
- YNAB-style table with columns: CATEGORY | ASSIGNED | ACTIVITY | AVAILABLE
  - Collapsible group rows with per-group totals and chevron toggle
  - Budget rows with icon/initial avatar, name, mini progress bar, inline-editable ASSIGNED, activity, AvailablePill
  - Inline editing: click assigned amount → input → Enter/blur commits → back-calculates native amount → PATCH `/api/budgets/:id` → refresh
  - "+ Add budget to [Group]" link → `/budgets?group=X`
  - "+ Add Group" link → `/budgets?newGroup=1`
  - Footer totals row
- Right panel: month summary card, overall progress bar, per-group breakdown bars, left-over placeholder

**`app/page.tsx`** — added `Plan` nav item to dashboard sidebar.

**`app/budgets/BudgetsClient.tsx`**:
- Added `defaultGroup` prop — auto-opens form and pre-fills group name (used by Plan page links)
- Added Group field to create budget form
- Budget list now shows `period · group_name` subtitle

**`app/budgets/page.tsx`**:
- Now accepts `searchParams: Promise<{ group?: string; newGroup?: string }>`
- Passes `defaultGroup` to BudgetsClient

## Bug fixes — DB column errors + isValidViewPeriod

**Supabase errors observed in production:**
- `column "icon" does not exist` — `icon` was in the original migration but missing from live DB
- `column "group_name" does not exist` — migration 20240007 not yet applied

**Fix:** Migration 20240008 (`supabase/migrations/20240008_add_missing_bucket_columns.sql`) adds `icon`, `color`, `currency`, and `group_name` to `buckets` using `IF NOT EXISTS` — supersedes running 20240007 separately.

**`isValidViewPeriod` client-server error:**
- Error was from a stale Vercel deployment, not the current code
- `lib/period.ts` has no `"use client"` directive; local `.next` build confirmed correct
- Resolved by the fresh Vercel deployment triggered by latest push

## DB fixes applied directly via postgres driver
- `group_name` column added to `buckets` — fixed Plan/Dashboard query crash
- `icon` column added to `categories` — fixed Budgets page crash
- `budget_period` enum extended with biennial/triennial/quinquennial/decennial — all 9 values now live

## Categories + All Accounts (2026-05-25 afternoon)

### What was built

**`app/budgets/BudgetsClient.tsx`** — split into Budgets | Categories tabs:
- Budgets tab: existing budget list + create form (group field, fixed `?newGroup=1` auto-open bug)
- Categories tab: list + create form (emoji icon + name) + delete, calls `POST /api/categories`

**`app/api/categories/[id]/route.ts`** (NEW) — `DELETE` handler for category deletion.

**`app/review/ReviewCard.tsx`** — Category field changed from `<select>` to `<input list>` + `<datalist>` so users can type any category name or pick from existing ones.

**`app/accounts/page.tsx`** (NEW) — All Accounts page:
- 3-column layout with navy sidebar (All Accounts active)
- Summary strip: Total Spending | Transactions | Confirmed | Needs Review
- Desktop table: Date | Payee | Budget | Category | Source | Outflow
- Mobile: compact card layout
- Grouped by date with date subheaders
- Clicking any row → Review page

**Sidebar updates** — All Accounts (⬡) added to Dashboard and Plan sidebars.

## Still pending
- PWA icons
- Mobile sidebar / bottom nav
- Filter bar on Plan page (All / Underfunded / Overfunded)
- Rollover "Left Over from Last Month" in Plan right panel
